/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'Nikita Skvortsov' at '21.02.14 16:46' with Gradle 1.11
 *
 * This generated file contains a commented-out sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at http://gradle.org/docs/1.11/userguide/tutorial_java_projects.html
 */

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:0.6+"
    }
}

def kotlin_version = "0.1-SNAPSHOT"

group = "org.jetbrains.kotlin.buildtool"
version = "0.1-SNAPSHOT"

apply plugin: 'kotlin'
apply plugin: 'maven'
apply plugin: 'idea'

// In this section you declare where to find the dependencies of your project
repositories {
    // Use 'maven central' for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    mavenCentral()
    mavenLocal()
}

sourceSets {
    integrationTest
}

configurations {
    integrationTestCompile {
        extendsFrom compile
    }
}

// In this section you declare the dependencies for your production and test code
dependencies {
    compile gradleApi()
    compile "org.jetbrains.kotlin:kotlin-compiler:${kotlin_version}"
    compile "org.jetbrains.kotlin:kotlin-stdlib:${kotlin_version}"
    compile "com.google.guava:guava:14.0.1"
    compile "commons-io:commons-io:1.4"

    integrationTestCompile "junit:junit:4.11"
}

task intTest(type: Test, dependsOn: "install") {
    testClassesDir = sourceSets.integrationTest.output.classesDir
    classpath = sourceSets.integrationTest.runtimeClasspath
}

compileKotlin {
    kotlinOptions.annotations = "annotations"
}

compileIntegrationTestKotlin {
    kotlinOptions.annotations = "annotations"
}

